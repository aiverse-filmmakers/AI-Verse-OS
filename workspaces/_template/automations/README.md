# Workspace Automations

Workspace-scoped automation definitions may live here when they are tightly coupled to this workspace.

They should declare trigger/schedule, inputs, capability, permissions, validation, failure behavior, output destination, and approval policy.

Promote to root `automations/` only when the automation is genuinely shared.
