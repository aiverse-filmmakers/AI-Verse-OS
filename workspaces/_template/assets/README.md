# Workspace Assets

Store or route to files needed by this workspace: source documents, media, datasets, design assets, code artifacts, templates, or other reusable inputs.

Large or externally managed assets should usually remain in their authoritative system with a durable route recorded in `WORKSPACE.yaml` or the connection registry.
