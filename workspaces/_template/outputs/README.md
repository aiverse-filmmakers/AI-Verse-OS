# Workspace Outputs

Use for durable deliverables produced by the workspace when keeping them locally is useful.

Temporary intermediates, caches, previews, and machine-generated scratch files should prefer `runtime/` or an ignored local output path.

An output is not automatically a source of truth for the facts it contains.
