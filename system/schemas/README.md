# Schemas

Machine-readable contracts used by AI-Verse OS live here.

Schemas should define universal structure without closing the system around a fixed list of professions or industries.

The workspace schema deliberately allows free-form `type` and `domains` values.
