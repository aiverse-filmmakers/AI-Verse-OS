# Workspace Memory

Store workspace history, dated events, learnings, and handoffs that may matter later but should not be loaded into every request.

Keep `MEMORY.md` compact and use deeper dated files as the workspace grows.
