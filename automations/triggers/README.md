# Triggers

Event-trigger definitions live here: new data, incoming messages, file changes, source updates, thresholds, or other future conditions.

A trigger should identify its source, scope, deduplication behavior, and what capability is allowed to run.
