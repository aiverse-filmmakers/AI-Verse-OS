# System Templates

Reusable domain-neutral scaffolds belong here when multiple parts of the OS need them.

The canonical workspace scaffold currently lives in `workspaces/_template/` so it is visible where users create workspaces. Its contract is validated by `system/schemas/workspace.schema.yaml`.

Do not create profession-specific templates in the universal core. Optional domain packs may provide additional workspace templates later.
