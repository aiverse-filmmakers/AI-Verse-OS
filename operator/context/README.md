# Operator Current Context

`CURRENT.md` is the compact cross-workspace startup brief.

It should contain only what is useful now:

- current priorities
- active workspaces and why they matter
- pending cross-workspace decisions
- temporary constraints
- pointers to deeper sources

It should not contain full histories, raw transcripts, or every fact known about the operator.
