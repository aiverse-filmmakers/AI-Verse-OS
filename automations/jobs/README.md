# Jobs

Scheduled or repeated automation definitions live here.

Do not confuse a file named `daily` with proof that anything actually runs daily. `/audit` should look for runtime evidence when verifying cadence.
